# coding: utf-8

"""
    Catalog API

    This API lists data sets available on Agrimetrics platform.

    Generated by: https://konfigthis.com
"""

from datetime import datetime, date
import typing
from enum import Enum
from typing_extensions import TypedDict, Literal, TYPE_CHECKING
from pydantic import BaseModel, Field, RootModel, ConfigDict


class DataRequest(BaseModel):
    # The name of the project the data is for
    project_name: str = Field(alias='projectName')

    # The name of the project manager who can approve the request
    project_manager_name: str = Field(alias='projectManagerName')

    # The email address of the project manager who can approve the request
    project_manager_email: str = Field(alias='projectManagerEmail')

    # The ID of the dataset that the data request relates to
    dataset_id: str = Field(alias='datasetId')

    # The name of the dataset that the data request relates to
    dataset_name: str = Field(alias='datasetName')

    # The format the user would like the data to be provided in
    requested_format: Literal["application/geo+json", "application/gml+xml; version=3.2", "application/vnd.google-earth.kml+xml", "application/zipped-shapefile", "application/x.gdb+zip", "application/x.mid-mif+zip", "application/x.tab+zip", "application/dxf", "image/tiff"] = Field(alias='requestedFormat')

    # The area of interest that the user is requesting access to as a stringified GeoJSON
    geo_json_a_o_i: typing.Optional[str] = Field(None, alias='geoJsonAOI')

    # The area of interest that the user is requesting access to as a zipped shapefile
    shapefile_a_o_i: typing.Optional[typing.IO] = Field(None, alias='shapefileAOI')

    model_config = ConfigDict(
        protected_namespaces=(),
        arbitrary_types_allowed=True
    )
